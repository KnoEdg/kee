# JSON Schema

This directory contains draft JSON Schema artifacts for KEE metadata and profiles.

Planned schemas:

- profile metadata;
- lifecycle state definitions;
- governance metadata;
- provenance metadata;
- profile conformance declarations.

Schemas are draft unless referenced by the KEE Specification or accepted ADR.
