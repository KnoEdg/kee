# KEE Reference Implementation

The Python reference implementation (the `kee` CLI: `validate`, `normalize`, `conformance`, and `query`) is not yet published to this repository.

This directory currently holds only the license that will govern that code once it is: the GNU Affero General Public License, Version 3 (`reference/LICENSE`).
